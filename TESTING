Blake

clisp
> (load "crysp-blake2b.lisp")
> (test1)
> (quit)

HMAC

clisp
> (load "crysp-sha-512.lisp")
> (load "crysp-hmac.lisp")
> (test1)
> (quit)

SipHash

clisp
> (load "crysp-siphash.lisp")
> (test1)
> (quit)
